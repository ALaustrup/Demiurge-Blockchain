[package]
name = "sc-network"
version = "0.38.0"
edition = "2021"
license = "Apache-2.0 OR MIT"

[lib]
path = "lib.rs"

[dependencies]
parity-scale-codec = { version = "3.6", features = ["derive"] }
