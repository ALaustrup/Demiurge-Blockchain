{
  "name": "@demiurge/wallet-wasm",
  "version": "0.1.0",
  "private": true,
  "description": "WASM wallet utilities for CGT",
  "main": "./pkg/wallet_wasm.js",
  "types": "./pkg/wallet_wasm.d.ts",
  "files": [
    "pkg"
  ],
  "scripts": {
    "build": "wasm-pack build --target web --out-dir pkg",
    "clean": "rm -rf pkg"
  },
  "devDependencies": {
    "wasm-pack": "^0.12.1"
  }
}
